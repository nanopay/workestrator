{
  "name": "nano-workestrator",
  "author": "anarkrypto@gmail.com",
  "scripts": {
    "dev": "wrangler dev src/index.ts --port 7090",
    "deploy": "wrangler deploy --minify src/index.ts",
    "format": "prettier --config .prettierrc.json --write ."
  },
  "dependencies": {
    "hono": "^3.2.7",
    "nanocurrency": "^2.5.0",
    "prettier": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "wrangler": "^3.1.0"
  }
}